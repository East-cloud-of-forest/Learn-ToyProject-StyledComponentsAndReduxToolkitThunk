import styled from "styled-components";

const Board = () => {
  return (
    <StBoard>
      <h1>
        게시판
      </h1>
    </StBoard>
  );
};

const StBoard = styled.main`
  margin-top: 7rem;
  text-align: center;
`;

export default Board;
